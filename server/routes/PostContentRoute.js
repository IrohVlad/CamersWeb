import { Router } from "express";
import authChech from "../middleware/authChech.js";

const router = Router();

router.get('content', );
router.post('content', );
router.putch('content', );
router.delete('content', );